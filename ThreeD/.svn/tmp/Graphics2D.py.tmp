import curses

pixel = ' '

# TODO: Make an orange color using init_color
# TODO: color doesn't work with windows pdcurses =(
curses.init_pair(1, curses.COLOR_RED, curses.COLOR_YELLOW)
white = curses.color_pair(1)

# If you're using this library, we assume that you've called curses.start_color() first
def draw_line(screen, start, end):
    global pixel, white
    screen.addch(start[1], start[0], pixel, white)
    
    if start[0] > end[0]:
        start, end = end, start
    slope = (start[1] - end[1]) / (1.0 + start[0] - end[0])
<<<<<<< .mine
    y = start[1]
    
    for x in range(start[0], end[0]):
#        screen.addstr(8, 10, str(x) + " * " + str(round(y)))
#        screen.getch()
        
        screen.addch(round(y), x, pixel, white)
        y += slope
=======

    if (abs(start[0] - end[0]) > abs(start[1] - end[1])):
        y = start[1]
        for x in range(start[0], end[0]):
            screen.addch(round(y), x, pixel, white)
            y += slope
    else:
        x = start[0]
        for y in range(start[0], end[0]):
            screen.addch(round(y), x, pixel, white)
            x += slope
>>>>>>> .r14
    screen.addch(end[1], end[0], pixel, white)
    screen.addstr(4, 4, str(curses.has_colors()))


def draw_polygon(screen):
    pass
